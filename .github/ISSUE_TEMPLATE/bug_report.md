---
name: Bug report
about: Something 9agent did that it shouldn't have
labels: bug
---

**What happened**

**What you expected instead**

**Command you ran**

```
9agent ...
```

**Environment**

- `9agent --version`:
- Agent (`claude` / `pi` / `hermes`) and its version:
- Host or `--sandbox`:
- OS, and Docker runtime if sandboxed (Docker Desktop / OrbStack / Linux):

**Output**

<!-- Redact first: never paste --key, NINEROUTER_KEY, or LOCAL_9ROUTER_KEY. A gateway key is a credential. -->

```
```
